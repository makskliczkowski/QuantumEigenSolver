# QES JAX dependencies (safe for pre-installed GPU JAX)

# Do NOT install jax or jaxlib here — install manually for GPU (see below)
# jax>=0.4.23
# jaxlib>=0.4.0

# Install manually: https://github.com/google/jax#installation
# pip install --upgrade "jax[cuda12_local]" -f https://storage.googleapis.com/jax-releases/jax_cuda_releases.html

# ML and optimization libraries (GPU-safe)
flax>=0.6.0
optax>=0.1.0
